# autonomy-loop — launch copy

Two ways to post. Both lead with the video. Both 100% true. Pick one.

═══════════════════════════════════════════════════════════════════
## OPTION A — ONE TWEET (attach the video)   ← simplest, highest reach
═══════════════════════════════════════════════════════════════════

Two AIs. One repo. Nobody driving.

I went to the pool. It shipped 5 reviewed features — output byte-identical — then proposed and built a 6th on its own.

Free Claude Code plugin, MIT 👇
/plugin marketplace add https://github.com/inferencegod/autonomy-loop

> (Repo in the first reply: github.com/inferencegod/autonomy-loop)


═══════════════════════════════════════════════════════════════════
## OPTION B — 5-TWEET THREAD   ← more depth, still tight
═══════════════════════════════════════════════════════════════════

**1/  (attach the video)**
Two AIs. One repo. Nobody driving.

I left for the pool — it shipped 5 reviewed features (output byte-identical), then proposed and built a 6th on its own.

A free Claude Code plugin. how it works 🧵

**2/**
Two Claude Code terminals — a Builder and an adversarial Reviewer — pass a git baton.

The reviewer runs 5 lenses: correctness, honesty, regression, security, UX. All must pass, and it red-teams its own verdict first. Not a rubber stamp.

**3/**
It can't wreck your repo.

A hook hard-blocks prod push, force-push, history rewrite, and edits to protected files — and a frozen-invariant gate keeps your money path byte-identical until YOU re-baseline.

**4/**
Two things most agents won't do:

It won't fabricate — every stat carries its sample size, or says "building — N/30."
It won't idle — empty backlog? it researches, proposes, and builds the next feature itself.

**5/**
Install (Claude Code) · MIT · open source:

/plugin marketplace add https://github.com/inferencegod/autonomy-loop
/plugin install autonomy-loop@autonomy-loop

Star it ⭐ Reply with what it ships while you're AFK. 🛠️
→ github.com/inferencegod/autonomy-loop


═══════════════════════════════════════════════════════════════════
## EXTRAS
═══════════════════════════════════════════════════════════════════

**Alt hook (contrarian — rips with devs):**
Most AI agents break your repo and lie about it. I built the opposite — a builder + an adversarial reviewer that ship gated code on autopilot and refuse to fabricate a single stat. Free + MIT. 🧵

**One-liner (bio / Product Hunt):**
Two Claude Code terminals — a builder + an adversarial reviewer — ship gated, no-fabrication code on autopilot, and invent the next feature when the backlog's empty. Free, MIT.

**Hashtags (final reply only):** #ClaudeCode #buildinpublic

**Reply-bait (boosts dwell time):**
"Ask me what the adversarial reviewer caught that I would've shipped."


═══════════════════════════════════════════════════════════════════
## DEEP CUT — the full 11-tweet version (only if you want maximum detail)
═══════════════════════════════════════════════════════════════════

1/ (video) Two AIs. One repo. Nobody driving. I left for the pool — it shipped 5 reviewed features (byte-identical) and built a 6th on its own. Free Claude Code plugin. 🧵
2/ It's my real 1,200+ test codebase, not a demo. I was AFK; it shipped 5 features through an adversarial gate, frozen invariant byte-identical, then ran out of tasks and invented the next.
3/ Two terminals: Builder writes code + a RED→GREEN test, runs the gate, commits. Reviewer re-runs the gate, tears the diff apart, fixes or flags. They pass a git baton. You stop prompting.
4/ The reviewer runs 5 lenses — correctness, honesty, regression, security, UX — all must pass. Before any PASS it red-teams the opposite and re-breaks your test to prove it bites.
5/ Safety hook on every tool call: blocks prod push, force-push, history rewrite, protected-file edits. Frozen-invariant gate keeps the money path byte-identical until you re-baseline.
6/ Anti-fabrication baked in: every stat carries its sample size or says "building — N/30." A feature with no data abstains, visibly. It can't lie to you about wins.
7/ Empty backlog isn't a stop — it researches competitors + your code, proposes features, builds the safe ones, parks the risky ones for sign-off. It feeds itself. That's how the 6th got built.
8/ Builder/reviewer loops aren't new. The new part is the discipline, installable: multi-lens review + frozen-invariant gate + anti-fabrication + per-project config. Value = constraints, not novelty.
9/ Tune it in one config: work branch, protected paths, gate command, models. Drop it into any repo.
10/ /plugin marketplace add https://github.com/inferencegod/autonomy-loop  then  /plugin install autonomy-loop@autonomy-loop  · MIT.
11/ If you've watched an agent break your repo and lie about it — this is the opposite. Try it, star it, reply with what it ships while you're AFK. → github.com/inferencegod/autonomy-loop
